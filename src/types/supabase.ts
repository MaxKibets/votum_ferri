// TODO: Replace with generated Supabase types later (e.g. via `supabase gen types typescript`)
// Temporary definitions to keep SDD and codebase consistent

export interface SupabaseProfile {
  id: string;
  userId: string;
  createdAt: string;
  updatedAt: string;
}

